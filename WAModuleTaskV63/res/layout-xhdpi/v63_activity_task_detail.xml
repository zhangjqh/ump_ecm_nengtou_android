<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/common_bg"
    android:orientation="vertical" >

    <include layout="@layout/title_panel" />
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="fill_parent"
            android:orientation="vertical"
            android:layout_marginBottom="49dp" >
    <ScrollView
        android:id="@+id/taskDetail_content_panel"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:paddingTop="@dimen/commonPadding"
        android:paddingBottom="@dimen/commonPadding" >
            </ScrollView>
      </LinearLayout>
    <LinearLayout
        android:id="@+id/task_detailbottom"
        android:layout_width="wrap_content"
        android:layout_height="49dp"
        android:layout_marginTop="-49dp"
        android:orientation="vertical" >

        <include layout="@layout/task_detail_bottom" />
    </LinearLayout>

</LinearLayout>
